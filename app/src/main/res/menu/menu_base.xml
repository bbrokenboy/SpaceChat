<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/mnChats"
        android:contentDescription="Chats"
        android:enabled="true"
        android:icon="@drawable/ic_chats"
        app:showAsAction="always"
        android:title="Chats"
       />
    <item
        android:id="@+id/mnMarketPlace"
        android:contentDescription="MarketPlace"
        android:enabled="true"
        android:icon="@drawable/ic_marketplace"
        app:showAsAction="always"
        android:title="MarketPlace"
        />
    <item
        android:id="@+id/mnPerfil"
        android:icon="@drawable/ic_account_img"
        android:title="Perfil"
        app:showAsAction="always"
        android:enabled="true"
        android:contentDescription="Perfil"

        />
</menu>